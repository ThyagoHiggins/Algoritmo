Algoritmo "tabuada"

Var
 cont, N, R : inteiro

Inicio

      cont <- 0
      escreva ("qual número voce quer ver a tabuada?")
      leia (N)
      
           repita
           R <- N*cont
           escreval (N, "x", cont, "=", R)
           cont <- cont + 1
           
           ate (cont >10)


Fimalgoritmo